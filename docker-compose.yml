services:
  postgres:
    image: postgres:16
    container_name: portfolio-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: portfolio_analytics
      POSTGRES_USER: dalton
      POSTGRES_PASSWORD: localdev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
